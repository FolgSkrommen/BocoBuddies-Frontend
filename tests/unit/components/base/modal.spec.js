import { mount, shallowMount } from '@vue/test-utils'
import { BaseModal } from '@/components/Base/BaseModal'

describe('Tests for BaseModal', () => {
	it('Check that props are being rendered', () => {
		let showModal = false
	})
})
